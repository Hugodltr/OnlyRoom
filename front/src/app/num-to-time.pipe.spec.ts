import { NumToTimePipe } from './num-to-time.pipe';

describe('NumToTimePipe', () => {
  it('create an instance', () => {
    const pipe = new NumToTimePipe();
    expect(pipe).toBeTruthy();
  });
});
